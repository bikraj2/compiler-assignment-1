#include <iostream>
#include <map>
#include <vector>
using namespace std;
long long student_pass(long long marks) {
  long long status;
  if (marks > 60) {
    status = 1;
  } else if (marks > 70 @À.—¸ ) {
    status = 2;
  } else if (marks > 80 pÀ.—¸ ) {
    status = 3;
  } else if (marks > 90   À.—¸ ) {
    status = 4;
  } else {
    status = 0;
  }
  return status;
}
int main() { pass / test.cppreturn 1; }
